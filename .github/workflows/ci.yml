name: CI

'on':
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  format-gate:
    name: Code Format Gate (clang-format)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install clang-format
        run: |
          sudo apt-get update
          sudo apt-get install -y clang-format
      - name: Run code formatting check
        run: ./scripts/dev.sh format --check

  linux-test-gate:
    name: Linux Clang Build & Unit Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository with submodules
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            cmake ninja-build clang lld pkg-config \
            libasound2-dev libjack-jackd2-dev libfreetype-dev libfontconfig1-dev \
            libx11-dev libxcomposite-dev libxcursor-dev libxext-dev \
            libxinerama-dev libxrandr-dev libxrender-dev libgl1-mesa-dev

      - name: Run unit test suite (devpiano_tests)
        run: ./scripts/dev.sh test

  windows-msvc-gate:
    name: Windows MSVC Build & Unit Tests
    runs-on: windows-latest
    steps:
      - name: Checkout repository with submodules
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up MSVC Developer Command Prompt
        uses: ilammy/msvc-dev-cmd@v1

      - name: Set up Ninja
        uses: ninja-build/setup-ninja@v5

      - name: Configure CMake (windows-msvc-debug)
        run: cmake --preset windows-msvc-debug -DBUILD_TESTS=ON

      - name: Build target binaries (Debug)
        run: cmake --build --preset windows-msvc-debug

      - name: Run Windows unit test suite
        run: ctest --test-dir build-win-msvc --output-on-failure
