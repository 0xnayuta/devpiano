# devpiano 阶段里程碑验收清单（初版）

> 用途：本文件用于定义各阶段的可验证验收标准，避免重构过程中只“写了代码”但没有形成可确认的阶段成果。
>
> 状态标记说明：
> - [x] 已通过
> - [ ] 未通过 / 未开始验证
> - [~] 部分通过 / 待补充验证

---

## 1. 当前里程碑总览

### M0：工程骨架可运行
- [x] JUCE GUI 程序可正常启动
- [x] Debug 构建可通过
- [x] 主窗口可显示
- [x] 音频设备能初始化

### M1：最小演奏链路成立
- [x] 电脑键盘可触发 note on/off
- [x] 虚拟钢琴键盘可联动显示
- [x] 外部 MIDI 输入可打开
- [x] 程序能够发出声音（当前为内置正弦波）

### M2：最小插件扫描能力成立
- [x] 可识别插件格式管理器
- [x] VST3 格式可用
- [x] 可扫描默认或指定目录
- [x] 可显示扫描到的插件列表

### M3：插件实例化并发声
- [ ] 可选择并加载一个 VST3 乐器
- [ ] 键盘输入可驱动该插件发声
- [ ] 外部 MIDI 输入可驱动该插件发声
- [ ] 插件卸载后程序仍稳定

### M4：键盘映射系统可配置
- [ ] 映射不再依赖字符输入
- [ ] 映射可保存
- [ ] 映射可恢复
- [ ] 默认布局可重置

### M5：UI 进入正式可用阶段
- [ ] 插件状态显示清晰
- [ ] 插件选择/加载/卸载路径完整
- [ ] `MainComponent` 职责得到初步拆分
- [ ] 用户无需依赖调试性文本框理解状态

### M6：高级功能恢复
- [ ] 支持布局 Preset 保存/加载
- [ ] 支持录制与回放
- [ ] 支持 MIDI 或 WAV 导出

---

## 2. 近期重点里程碑验收项

## M0：工程骨架可运行

### 验收目标
确保当前主干具备稳定开发基础。

### 验收项
- [x] `cmake --build Build/vs2026-x64 --config Debug` 构建成功
- [x] 生成 `DevPiano.exe`
- [x] 程序启动后主窗口正常显示
- [x] 没有因缺失 JUCE 子模块导致构建失败

### 验收记录
- 当前状态：已通过
- 备注：可作为后续迭代的基础基线

---

## M1：最小演奏链路成立

### 验收目标
确保“输入 -> MIDI -> 声音输出”的基本闭环存在。

### 验收项
- [x] 按下 `A/S/D/F` 等基础按键时可触发 note on
- [x] 松开对应按键时可触发 note off
- [x] 虚拟钢琴键盘组件可高亮联动
- [x] 外部 MIDI 设备打开后，消息能进入音频引擎
- [x] 调整音量与 ADSR 滑块后可听到参数变化

### 待补充验证
- [ ] 切换窗口焦点后 held key 是否会残留
- [ ] 长按某按键时是否出现异常重复触发

### 验收记录
- 当前状态：部分通过（基础闭环已成立，稳定性验证待补）

---

## M2：最小插件扫描能力成立

### 验收目标
确保项目已经具备插件生态入口，而非纯内置演示程序。

### 验收项
- [x] 程序能识别可用插件格式
- [x] `PluginHost` 能报告 VST3 可用状态
- [x] 输入默认 VST3 搜索路径后可执行扫描
- [x] 扫描完成后可在界面列出插件名称
- [x] 扫描失败不会导致程序崩溃

### 待补充验证
- [ ] 扫描结果是否能持久化
- [ ] 扫描失败文件是否能正确记录
- [ ] 多目录搜索路径是否表现稳定

### 验收记录
- 当前状态：部分通过（最小扫描能力已成立，持久化与错误处理待完善）

---

## M3：插件实例化并发声

### 验收目标
完成当前最关键的产品级突破：从“会扫描插件”升级为“真正使用插件发声”。

### 验收项
- [ ] 扫描结果中可选择一个 VST3 乐器
- [ ] 点击加载后能成功创建 `AudioPluginInstance`
- [ ] 插件加载成功后 UI 显示明确状态
- [ ] 按下 `A/S/D/F` 等按键时插件能够发声
- [ ] 外部 MIDI 输入也能驱动插件发声
- [ ] 插件处理链路接入 `processBlock`
- [ ] 插件卸载后不会崩溃或留下非法状态
- [ ] 未加载插件时程序仍保持可运行

### 建议验证插件类型
- [ ] 选择一个简单的 VST3 乐器作为首测目标
- [ ] 选择一个加载较慢的插件测试状态反馈
- [ ] 选择一个加载失败的插件测试错误提示

### 验收记录
- 当前状态：未开始
- 备注：这是近期最高优先级里程碑

---

## M4：键盘映射系统可配置

### 验收目标
把当前硬编码键盘映射升级为可持续演进的用户输入系统。

### 验收项
- [ ] 映射内部模型不再只是 `unordered_map<int, int>`
- [ ] 映射不再强依赖 `key.getTextCharacter()`
- [ ] 默认布局可加载
- [ ] 当前布局可保存到设置
- [ ] 重启程序后布局可恢复
- [ ] 可恢复默认布局
- [ ] 焦点切换后按键状态仍稳定

### 建议手工用例
- [ ] `A/S/D/F/G/H/J` 行映射连续正确
- [ ] `Q/W/E/R/T/Y/U` 行映射连续正确
- [ ] `Z/X/C/V/B/N/M` 行映射连续正确
- [ ] 保存布局后重启程序验证一致性

### 验收记录
- 当前状态：未开始
- 备注：这是近期最高优先级里程碑之一

---

## M5：UI 进入正式可用阶段

### 验收目标
让当前界面从“调试面板”迈向“可用产品界面”。

### 验收项
- [ ] 插件扫描、选择、加载、卸载路径完整
- [ ] 插件状态标签表达清晰
- [ ] MIDI 状态反馈清晰
- [ ] 用户可以理解当前发声来源（内置 / 插件）
- [ ] `MainComponent` 已有局部职责拆分
- [ ] 主要交互控件布局稳定，不明显拥挤

### 待补充项
- [ ] 插件 editor 打开路径
- [ ] 更好的错误提示与空状态提示

### 验收记录
- 当前状态：未开始

---

## M6：高级功能恢复

### 验收目标
基于新架构逐步恢复旧版实用功能。

### 验收项

#### 布局 Preset
- [ ] 可保存当前键盘布局
- [ ] 可加载历史布局
- [ ] 可恢复默认布局

#### 录制 / 回放
- [ ] 可开始录制
- [ ] 可停止录制
- [ ] 可回放录制结果
- [ ] 回放期间事件时间顺序正确

#### 导出
- [ ] 可导出 MIDI
- [ ] 可导出 WAV
- [ ] 导出失败时有清晰错误提示

### 验收记录
- 当前状态：未开始

---

## 3. 近期建议作为“完成标准”的最小集合

如果下一轮目标是明确、聚焦并且可交付，建议把以下项目视为最近一次迭代的“完成标准”：

### 最小完成标准（建议优先）
- [ ] 能扫描到至少一个 VST3 插件
- [ ] 能从扫描结果中选择并加载一个 VST3 乐器
- [ ] `A/S/D/F` 能驱动该插件发声
- [ ] 外部 MIDI 输入能驱动该插件发声
- [ ] 卸载插件后程序保持稳定
- [ ] 键盘映射可保存并恢复

---

## 4. 每轮迭代结束时建议补充的记录项

每轮迭代完成后，建议在本文件补充：

- [ ] 本轮目标
- [ ] 实际完成项
- [ ] 未完成原因
- [ ] 新发现风险
- [ ] 下一轮入口任务

可按以下模板追加：

```md
## Iteration YYYY-MM-DD
- 目标：
- 完成：
- 未完成：
- 风险：
- 下一步：
```

这样可以让本文件同时具备“里程碑验收表”和“阶段推进日志”双重作用。
